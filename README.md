# Neural Deterministic Finite-state Automaton

Implements a DFA consisting entirely of spiking LIF neurons (except bias nodes) in `nengo`.

## Setup

```sh
uv init
uv add nengo nengo_spa nengo_gui numpy scipy jupyterlab matplotlib
uv run nengo demo.py
```
